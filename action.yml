name: "Cancel workflow"
author: "action-pack"
branding:
  icon: "alert-octagon"
  color: "gray-dark"
description: "Action to cancel a workflow."

inputs:
  token:
    description: 'Token'
    required: false
    default: ${{ github.token }}

runs:
  using: 'node20'
  main: 'index.js'

